// The SDK is allowance-kit's SDK, re-exported unchanged. Import from either name:
//   import { payingFetch, createAgent } from "wallie";
//   import { payingFetch, createAgent } from "allowance-kit";
export * from "allowance-kit";
